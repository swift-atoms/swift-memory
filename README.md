
## Semantic ownership

Memory owns addresses, alignment, regions and memory quantities. Page sizes describe memory alignment granularity; platform discovery belongs to an integration. Memory.Capacity is a byte count and its UInt64 conversion preserves the full unsigned range. The broader allocation boundary remains under review.
